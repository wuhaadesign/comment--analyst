# OPTIMIZATION.md 模板（douyin-comment-collector 首次启动自检记录）

> **位置固定**：本文件生成后写入用户本地项目文件夹根目录 `douyin-comment-collector/OPTIMIZATION.md`。
> **一个 Agent + 一个运行环境一条记录，追加不覆盖**；换 Agent 或换环境时在文件末尾**新增一条**记录，不要改动历史记录。
> **优化原则**：自检只减少「用户手动操作量」，绝不降低「结果可靠性」。任何需要绕过抖音登录态 / 逆向签名 / 臆造内容的所谓自动化一律不算优化，禁止启用。能力不确定时按「不可用」处理。
> **A 类护栏（A1–A3）永远保留人工**，状态列固定为「保留人工」，不参与「启用自动化」的确认。

---

## 记录 N（复制本区块追加，替换占位内容）

### 一、基本信息

| 项 | 内容 |
| --- | --- |
| Agent 标识 | <这个 Agent 的名称 / 版本 / 来源> |
| 运行环境 | <本机操作系统 / 是否有本地命令执行 / 是否有本地文件读写> |
| 空间 ID | <会话或工作空间标识，可选> |
| 自检时间 | <YYYY-MM-DD HH:MM 时区> |
| 触发原因 | 首次接入本 skill 采集流程 / 更换 Agent / 更换环境 |
| 记录状态 | 生效中 / 已被后续记录替代 |

### 二、能力探测结果

| 能力 | 结果（可用 / 不可用 / 不确定→按不可用） | 探测说明 |
| --- | --- | --- |
| 本地命令执行 | <可用 / 不可用> | <如何判定的> |
| 本地文件读写 | <可用 / 不可用> | <如何判定的> |
| 飞书多维表格写入 + 授权 | <可用 / 不可用> | <是否能以当前登录用户身份调 lark-base> |
| 外部模型 API（关键词拓展 / 评论分析 / 报告生成） | <可用 / 不可用> | <是否能调大模型> |

> 本 skill 无 VLM 读图 / 抽帧 / 转写步骤，**不探 VLM 能力**，故本表不含 VLM 行。

### 三、优化项确认结果

> A1–A3 状态固定「保留人工」；B1–B7 状态取「启用 / 保留人工 / 不适用」，需与用户逐项确认。

| 编号 | 人工点 | 类别 | 确认状态 | 依据 / 备注 |
| --- | --- | --- | --- | --- |
| A1 | 业务诉求确认（6 项输入 + 赛道 / 关键词种子） | A · 护栏 | 保留人工 | 赛道是分析硬锚点，必须人来定 |
| A2 | 抖音扫码登录 · 滑块 · 验证码 · 反风控长停顿 | A · 护栏 | 保留人工 | 无登录态触发 2483；绕过=逆向签名，违反铁律 |
| A3 | 飞书身份授权（以当前登录用户身份写表） | A · 护栏 | 保留人工 | 涉及身份凭证，不能代持 / 绕过 |
| B1 | 建项目文件夹 + 放脚本 + `ls` 自检结构 | B · 可优化 | <启用 / 保留人工 / 不适用> | 依赖：本地命令执行 + 本地文件读写 |
| B2 | 本地环境准备（Node / Chrome 探测 / `npm install`） | B · 可优化 | <启用 / 保留人工 / 不适用> | 依赖：本地命令执行 |
| B3 | 执行「一键采集 bash」 | B · 可优化 | <保留人工（需本机真机）/ 体验优化> | 无法代跑，只能优化 config 回填体验 |
| B4 | 产物 JSON 回传方式 | B · 可优化 | <启用 / 保留人工 / 不适用> | 依赖：本地文件读写（可直接读取，免手动拖文件） |
| B5 | 飞书建表 + 批量写入 | B · 可优化 | <启用 / 保留人工 / 不适用> | 依赖：飞书写入 + 授权 |
| B6 | 关键词 LLM 拓展 + 评论分析 + 瑞士极简报告生成 | B · 可优化 | <启用 / 保留人工 / 不适用> | 依赖：外部模型 API |
| B7 | 采集失败项 / 漏采视频补跑 | B · 可优化 | <启用 / 保留人工 / 不适用> | 依赖：本地文件读写（读产物 JSON 找缺口） |

### 四、生效的执行路径调整

> 逐条写明「本次自检后，相比默认手把手流程，哪些步骤由 Agent 代做 / 简化了」。例如：
> - B1 启用：由 Agent 生成一键建目录命令，用户无需手动逐个建文件夹。
> - B4 启用：Agent 直接读本地 `data/comments_*.json`，用户无需手动拖文件上传。
> - （未启用的项不写在此节。）

- <调整 1>
- <调整 2>

### 五、仍需用户参与的节点

> 至少包含 A1–A3，以及所有状态为「保留人工」的 B 类项。例如：
> - A1：填写 6 项诉求、确认赛道与关键词种子。
> - A2：本机扫码登录抖音、遇滑块 / 验证码手动处理、采集期间勿中断。
> - A3：确认以当前登录用户身份写飞书。
> - B3：在本机终端粘贴运行「一键采集 bash」。

- <节点 1>
- <节点 2>

### 六、备注

<本次自检的其他说明：能力边界、失败回退方案、下次复检条件等。>
